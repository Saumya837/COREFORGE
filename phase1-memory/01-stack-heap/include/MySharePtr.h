#include<iostream>
/*
    The shareUniquePtr is a simple implementation of a unique Pointer in c++.
    It is a template class that can manage any type of pointer, and it ensueres single ownership of a pointer at any time.
    The main features of myUniquePtr are:
        1. It takes ownership of a heap pointer and deletes it when the MyUniquePtr goes out of scope.
        2. It supports move semantics, allowing ownership to be transferred but not copied.
        3. It provides dereference and arrow operators to access the underlying pointer's members. 
*/



namespace codespace{
    template <typename t>
    class mySharePtr{
        private:

        public:

    }
}

